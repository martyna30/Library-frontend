import {AdDirective} from './ad.directive';


describe('Ad.DirectiveDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new AdDirective();
    expect(directive).toBeTruthy();
  });
});
